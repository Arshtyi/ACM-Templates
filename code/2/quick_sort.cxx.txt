void qsort(int a[], int l, int r)
{
    int i = l, j = r, flag = a[r + ((r - l) >> 1)], tmp;
    do
    {
        while (a[i] < flag) i++; while (a[j] > flag) j−−;
        if (i <= j) swap(a[i++],a[j--]);
    } while (i <= j);
    if (l < j) qsort(a, l, j); if (i < r) qsort(a, i, r);
}